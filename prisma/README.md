# Legacy SQLite schema

This directory is retained only to document and inspect the pre-Encore SQLite data model. Prisma is not a KaSiHUB runtime or build dependency. Production persistence is owned by the Encore databases under `encore/migrations`.

The preserved database and migration utilities must never be copied into a production image.
