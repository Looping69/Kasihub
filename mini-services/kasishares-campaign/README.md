# KaSiShares Campaign

Standalone frontend for the KaSiShares private-presale campaign.

## Local development

```powershell
npm install
npm run dev
```

The current reservation flow is deliberately frontend-only. It calculates a preview quote and opens a review dialog, but it does not create orders or provide payment instructions.
